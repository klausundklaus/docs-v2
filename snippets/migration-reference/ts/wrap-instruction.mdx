```typescript
import { createWrapInstruction } from "@lightprotocol/compressed-token";

const ix = createWrapInstruction(
  source,
  destination,
  owner.publicKey,
  mint,
  amount,
  splInterfaceInfo,
  decimals
);
```
